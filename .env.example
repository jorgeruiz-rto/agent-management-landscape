# Copy to .env and fill in. .env is gitignored — never commit the real key.
# Gravitee gateway key for the operations-hubspot-mcp-proxy API.
# Load locally with:  export $(grep -v '^#' .env | xargs)  &&  node hubspot-proof.js
HUBSPOT_MCP_API_KEY=
